import { FilteredToursPipe } from './filtered-tours.pipe';

describe('FilteredToursPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredToursPipe();
    expect(pipe).toBeTruthy();
  });
});
